<%= form_for(grandson) do |f| %>
  <% if grandson.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grandson.errors.count, "error") %> prohibited this grandson from being saved:</h2>

      <ul>
      <% grandson.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
